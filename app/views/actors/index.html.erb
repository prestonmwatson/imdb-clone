<h1>Actors</h1>

<h2>Click <%= link_to 'here', new_actor_path %> to add a new actor.
</h2>

<ul>
  <% @actors.each do |actor| %>
    <li>
      <%= link_to actor.name, actor_path(actor) %>
      <%= link_to(image_tag(actor.thumbnail), actor_path(actor)) %>
    </li>
  <% end %>
</ul>


