<h1>Movies</h1>

<h2>Click <%= link_to 'here', new_movie_path %> to add a new movie.
</h2>

<ul>
  <% @movies.each do |movie| %>
    <li>
      <%= link_to movie.title, movie_path(movie) %>
      <%= link_to(image_tag(movie.thumbnail), movie_path(movie)) %>
    </li>
  <% end %>
</ul>


